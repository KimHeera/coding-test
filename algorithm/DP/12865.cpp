#include <iostream>
#include <vector>

using namespace std;

#define MAX 100

int N, K;
vector<vector<int> > bag;
int list[MAX][2];


int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> N >> K;

    for (int i = 0; i < N; i++)
    {
        cin >> list[i][0] >> list[i][1];
    }

    for (int i = 0; i < N; i ++){
        for (int j = 0; j < K; j++){
            
        }
    }
}