#include <iostream>
#include <vector>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;

    int K;
    while (T--)
    {
        cin >> K;
        vector<int> file(K, 0);

          
    }
}